---
description: PetPal 性能目标（内存与空闲 CPU）
alwaysApply: true
---

# 性能目标（验收口径）

- **常驻内存**：典型空闲 / 后台场景 **< 50MB**（需可复现的构建与测量方式，避免无依据宣称）。
- **空闲 CPU**：无操作 / 后台时 **< 1%**（按团队约定的单核占比或采样方式记录）。

## 实现倾向

按需加载；避免常驻轮询；节制 WebView 与前端 `setInterval`/长任务；Rust 侧避免无谓线程与忙等；对热点路径做 profiling。
